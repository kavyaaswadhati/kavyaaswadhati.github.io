# Source Images

Keep original PNG artwork here, grouped by website section.

- `sketchbooks/` contains the source PNGs for the sketchbooks tab.
- `zines/` is ready for source PNGs for the zines tab.

Run `npm run images` after adding PNGs to regenerate optimized files in `public/images/`.
