# Northern Elephant Seals

Original single-page PNG files for this zine live here locally.

The `_original-spreads/` folder keeps the pre-split spread files as local backups. The image
generator skips folders whose names start with `_`.
